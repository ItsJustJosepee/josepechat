<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.JosephChat" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->

    </style>

    <style name="Theme.JosephChat.Splash" parent="Theme.SplashScreen">
        <!-- Icono que se mostrará -->
        <item name="windowSplashScreenAnimatedIcon">@mipmap/josephchat_foreground</item>
        <!-- Dura más si le agregas esto -->
        <item name="postSplashScreenTheme">@style/Theme.JosephChat</item>
    </style>

    <style name="Theme.JosephChat.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.JosephChat.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>